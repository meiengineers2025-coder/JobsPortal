<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DreamJobs Portal</title>

    <!-- Global CSS -->
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <header>
        <h1 class="brand">DreamJobs</h1>
        <nav>
            <% if (user) { %>
                <a href="/logout" class="btn-small">Logout</a>
            <% } else { %>
                <a href="/login" class="btn-small">Login</a>
            <% } %>
        </nav>
    </header>

    <main>
        <%- body %>
    </main>

    <footer>
        <p>© <%= new Date().getFullYear() %> DreamJobs — The World's Most Advanced AI Job Portal</p>
    </footer>
</body>

</html>
